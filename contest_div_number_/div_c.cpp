#include<bits/stdc++.h>
using namespace std;
int  main()
{
    string s;
    int sum=0;
   while(cin>>s)
   {
    for(int i=0;i<s.size();i++)
    {
        sum+=s[i];
    }
    while(sum/10>=0)
    {
        string str={sum(string)};
    }
   }
}